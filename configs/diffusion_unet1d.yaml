diffusion:
  seq_length: 1024
  feature_size: 24
  label_dim: 30
  timesteps: 50
  sampling_timesteps: 50
  loss_type: 'l2'
  beta_schedule: 'linear'
  use_ff: False



backbone:
  seq_len: 1024
  feature_dim: 24
  num_classes: 30
  hidden_ch: 64
  emb_dim: 32
  kernel_size: 5
  depth: 4

optimizer:
  lr: 0.0001
  betas: [0.9,0.99]

batch_size: 16